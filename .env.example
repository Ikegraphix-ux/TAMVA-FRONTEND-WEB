# Base URL of the real TAMVA backend/public API.
# Leave unset during development to use local placeholder content
# (see services/api.ts, services/products.ts, services/contact.ts).
NEXT_PUBLIC_API_URL=

# Public site URL, used for metadata, Open Graph tags and the sitemap.
NEXT_PUBLIC_SITE_URL=https://www.tamva.com
